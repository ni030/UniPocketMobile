import BNavigation from "components/general/BottomTabNavigator";
import { View } from "react-native";

export default function Layout() {
    return (
        <View className="flex-1">
            <BNavigation />
        </View>
    );
}
